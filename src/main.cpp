#include <iostream>
#include "server/server.h"
int main() { 
    std::cout << __cplusplus << "\n"; 

    // auto server = std::make_shared<Server>(IPVersion::IPv4, 9000);
    // server->run();

    return 0;
}
